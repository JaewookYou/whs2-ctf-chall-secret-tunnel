# 이 파일을 .env 로 복사하고 실제 플래그를 채운다.
#   cp .env.example .env
# .env 는 gitignore 되므로 실제 값이 저장소에 올라가지 않는다.
# 값을 바꾼 뒤에는 반드시 재빌드해야 이미지에 반영된다.
#   docker compose up -d --build
FLAG_EXTSERVER=flag{change_me_extserver}
FLAG_FLAGSERVER=flag{change_me_flagserver}
